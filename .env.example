# Canonical template: copy `.env.config.example` → `.env.config`, fill secrets, then:
#
#   pnpm env:sync          # → .env.local + apps/erp/.env.local
#   pnpm env:sync:cursor   # → Windows User env for Cursor MCP (optional)
#
# Maintainer source of truth is `.env.config` (gitignored). Do not commit secrets.
