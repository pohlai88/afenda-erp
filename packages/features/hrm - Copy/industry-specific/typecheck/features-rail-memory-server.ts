import type { RailMemorySavedView } from "./features-rail-memory"

type AnyValue = any

export const listSavedViewsForUser: (
  ...args: readonly AnyValue[]
) => Promise<readonly RailMemorySavedView[]> = null as AnyValue
