/** Stable DOM id for analytics CSV export (pairs with list-surface toolbar). */
export function engagementAnalyticsExportTriggerId(surveyId: string): string {
  return `engagement-analytics-export-trigger-${surveyId}`
}
